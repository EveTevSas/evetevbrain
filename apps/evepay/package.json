{
  "name": "@evetev/evepay",
  "version": "0.0.0",
  "private": true,
  "description": "evepay.evetev.com — landing del producto EvePay. Estático (HTML/CSS), se despliega en Vercel.",
  "scripts": {
    "dev": "python3 -m http.server 3004",
    "lint": "echo \"(evepay) sitio estático, sin lint\" && exit 0",
    "typecheck": "echo \"(evepay) sitio estático, sin typecheck\" && exit 0",
    "test": "echo \"(evepay) sitio estático, sin tests\" && exit 0"
  },
  "evetev": {
    "landing": true
  }
}
