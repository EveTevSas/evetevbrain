# Dependencias del agente. Va en la raíz de la app y no en api/ porque desde
# aquí se crea el entorno virtual y desde aquí lo lee el script `dev`.
#
# Versiones al día (sep-2026). Ojo: langchain-core exige pydantic>=2.7.4, así
# que no se puede fijar pydantic 2.5.x — la instalación falla.
# langchain-core se deja sin fijar a propósito: es dependencia transitiva de
# langgraph y langchain-openai, y que ellos resuelvan la versión compatible
# evita conflictos de pip.
fastapi==0.141.1
pydantic>=2.7.4,<3
python-dotenv==1.2.2
langchain-openai==1.4.2
langgraph==1.2.10
# El servidor ASGI: la interfaz y la API se sirven desde el mismo proceso.
uvicorn[standard]==0.42.0
