# Proyecto Supabase de EVEPAY: el Hub comparte la identidad (Google) con la consola.
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=""

# Base del Hub: MISMO Postgres que EvePay, rol hub_app (solo ve el schema hub).
# Local: postgresql://hub_app:postgres@127.0.0.1:57322/postgres
HUB_DATABASE_URL=""

# Dominios de correo que pueden entrar (separados por coma).
HUB_DOMINIOS_PERMITIDOS="evetev.com"

# API de EvePay para los roll-ups (local: apps/api).
NEXT_PUBLIC_API_URL="http://localhost:3011"
