# Proyecto Supabase de EVEPAY (no el de la vertical). Auth de la consola.
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=""

# API de EvePay (local: apps/api en :3001).
NEXT_PUBLIC_API_URL="http://localhost:3001"

# Solo para `pnpm auth:provision-admin` desde un entorno administrativo seguro.
# No se despliega en el runtime ni se expone al navegador.
SUPABASE_URL=""
SUPABASE_SECRET_KEY=""
SUPABASE_INVITE_REDIRECT_URL="http://localhost:3004/login"
