{
  "name": "@evetev/brand",
  "version": "0.0.0",
  "private": true,
  "description": "Marca Evetev: logotipos, isotipos, favicon, mascota y tokens de color. Fuente de verdad de la identidad.",
  "type": "module",
  "exports": {
    "./colores.json": "./assets/colores.json",
    "./colores.css": "./assets/colores.css",
    "./tokens/*": "./assets/tokens/*"
  },
  "files": ["assets"],
  "scripts": {
    "lint": "echo \"(brand) sin lint\" && exit 0",
    "typecheck": "echo \"(brand) sin typecheck\" && exit 0",
    "test": "echo \"(brand) sin tests\" && exit 0"
  }
}
