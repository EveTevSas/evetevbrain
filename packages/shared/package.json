{
  "name": "@evetev/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Contrato compartido de EvePay: tipos, DTOs y esquemas Zod. Una sola fuente de verdad.",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@evetev/config": "workspace:*",
    "typescript": "^5.7.3",
    "vitest": "^5.0.0"
  }
}
