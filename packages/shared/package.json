{
  "name": "@evetev/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Contrato compartido de EvePay: tipos, DTOs y esquemas Zod. Una sola fuente de verdad.",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "echo \"(shared) sin tests todavía\" && exit 0"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@evetev/config": "workspace:*",
    "typescript": "^5.7.3"
  }
}
