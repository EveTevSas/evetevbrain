# Tareas — RBAC operativo

- [x] R1 — Roles `ops` y `finanzas`, tabla de permisos y `exigir(accion)` en la API, con tests por acción (CA-2, CA-3).
- [x] R2 — Aplicar la tabla a todos los endpoints admin existentes y a los de dispersión; el rol en el detalle de la auditoría (CA-6).
- [x] R3 — Consola: acceso a los tres roles, `puede()` y botones por rol (CA-1); `provision-admin --role` (CA-5).
- [x] R4 — Cuatro ojos probado contra la base local (CA-4, con `dispersion` D3).
